To compile on windows type >"gcc main.c -o game.exe -lmingw32 -lSDL2main -lSDL2 -mwindows"
exclude the "-mwindows" if you want to see terminal debugging

To compile on linux type >"gcc main.c -o game -lSDL2"

thanks!